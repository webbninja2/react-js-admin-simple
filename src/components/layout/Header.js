import {Typography } from "@mui/material";

const PageHeader =({title})=>{

    return (
        <Typography variant="h4" className="head-title">{title}</Typography>
    )
}

export default PageHeader